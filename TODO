* Add context cancellation.
    - Make sure that the context we use have an associated deadline.
* Add more retry logic.
    - Exponential backoff.
    - Maximum number of attempts.
    - Customizable retry delay.
* Write actual real tests.
    - We probably need a Redis mock or something.